type User {
    id: ID!
    name: String!
    email: String!
}

type TblComments {
    id: ID!
    comment_title: String!
    comment_text: String!
}

type Query {
    user(id: ID! @eq): User @find
    users: [User!]! @all
    comments: [TblComments!]! @all
}
